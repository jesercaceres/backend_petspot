spring:
  application:
    name: petspot-restful-api
  datasource:
    url: jdbc:mysql://petspot-api-server.mysql.database.azure.com:3306/petspot?useSSL=true&requireSSL=false
    username: tafpegmkbq
    password: x$JX9aN1tS7w22uv
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5Dialect
  sql:
    init:
      platform: mysql
  flyway:
    baseline-on-migrate: true